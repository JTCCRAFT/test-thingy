name: Free money

on:
  push:
    branches:
      - main
  workflow_dispatch: # Allows you to run this manually from the Actions tab

jobs:
  say-it:
    runs-on: ubuntu-latest
    steps:
      - name: Decline Request
        run: echo "Absolutley not."
